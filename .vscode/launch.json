{
    "configurations": [
        
        
        {
            "type": "lldb",
            "request": "launch",
            "name": "Launch",
            "program": "${workspaceFolder}/Matrix_tests.exe",
            "args": [],
            "cwd": "${workspaceFolder}"
        }

    ]
}